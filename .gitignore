# Ajustes locales de Claude Code (privado)
.claude/

# Temporales de Python
__pycache__/
*.pyc

# Resultados de compilación
dist/
build/
bin/
.buildozer/
# OJO: NO ignorar *.spec en general, porque buildozer.spec debe subirse.
# Solo ignoramos el .spec temporal que genera PyInstaller:
IA_Anti_Ciberacoso.spec
